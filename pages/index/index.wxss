/* index.wxss */
page {
  font-family: "DIN Alternate", "Futura", "PingFang SC", sans-serif;
  width: 100vw;
  height: 100vh;
  overflow: hidden;
}

.page {
  width: 100vw;
  height: 100vh;
  padding: 2vh 2vw;
  background: linear-gradient(135deg, #f5f8ff 0%, #edf2ff 40%, #fef7f1 100%);
  box-sizing: border-box;
  overflow: hidden;
  display: flex;
  flex-direction: column;
  justify-content: center;
  align-items: center;
}

.top-bar {
  width: 96vw;
  max-width: 1200rpx;
  padding: 1.4vh 2vw;
  border-radius: 16rpx;
  background: #111827;
  color: #f9fafb;
  margin-bottom: 1.6vh;
  text-align: center;
  box-sizing: border-box;
}

.status-text {
  font-size: 32rpx;
  font-weight: 700;
}

.status-sub {
  margin-top: 6rpx;
  font-size: 24rpx;
  color: #e5e7eb;
}

.main {
  width: 96vw;
  max-width: 1200rpx;
  height: calc(100vh - 13vh);
  display: flex;
  gap: 1.6vw;
  overflow: hidden;
  box-sizing: border-box;
  align-items: center;
}

.left-panel {
  flex: 1;
  height: 100%;
  display: flex;
  flex-direction: column;
  gap: 1.4vh;
  justify-content: center;
}

.right-panel {
  flex: 1;
  height: 100%;
  display: flex;
  flex-direction: column;
  gap: 1.4vh;
  justify-content: center;
}

.search-area {
  display: flex;
  flex-direction: row;
  justify-content: center;
  gap: 1vw;
}

.search-btn,
.reconnect-btn,
.disconnect-btn,
.log-clear {
  border-radius: 12rpx;
  font-size: 24rpx;
  line-height: 1.2;
}

.search-btn,
.reconnect-btn {
  flex: 1;
}

.search-btn {
  background: #2563eb;
  color: #fff;
}

.reconnect-btn {
  background: #10b981;
  color: #fff;
}

.search-btn[disabled],
.reconnect-btn[disabled],
.disconnect-btn[disabled] {
  background: #cbd5f5;
  color: #6b7280;
}

.device-list {
  background: #ffffff;
  border-radius: 14rpx;
  padding: 12rpx;
  box-shadow: 0 8rpx 20rpx rgba(15, 23, 42, 0.08);
  max-height: 20vh;
  display: flex;
  flex-direction: column;
  overflow: hidden;
}

.list-title {
  font-size: 22rpx;
  color: #475569;
  margin-bottom: 8rpx;
  text-align: center;
}

.device-scroll {
  flex: 1;
  height: 100%;
}

.device-item {
  display: flex;
  flex-direction: column;
  gap: 4rpx;
  padding: 10rpx;
  background: #f8fafc;
  border-radius: 12rpx;
  margin-bottom: 8rpx;
}

.device-name {
  font-size: 24rpx;
  font-weight: 600;
  color: #111827;
}

.device-rssi {
  font-size: 20rpx;
  color: #64748b;
}

.log-area {
  background: #0f172a;
  border-radius: 16rpx;
  padding: 12rpx;
  color: #e2e8f0;
  flex: 1;
  display: flex;
  flex-direction: column;
  overflow: hidden;
}

.log-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 12rpx;
}

.log-title {
  font-size: 22rpx;
}

.log-clear {
  background: #f97316;
  color: #fff;
  padding: 6rpx 18rpx;
}

.log-list {
  flex: 1;
  min-height: 0;
}

.log-item {
  display: flex;
  gap: 10rpx;
  font-size: 20rpx;
  padding: 4rpx 0;
  border-bottom: 1rpx dashed rgba(148, 163, 184, 0.3);
}

.log-time {
  color: #94a3b8;
}

.log-hex {
  color: #f8fafc;
  word-break: break-all;
}

.joystick-area {
  display: flex;
  justify-content: center;
  align-items: center;
  gap: 2.4vw;
}

.joystick-block {
  flex: 1;
  background: #ffffff;
  border-radius: 18rpx;
  padding: 1.2vh 1.2vw;
  box-shadow: 0 10rpx 26rpx rgba(15, 23, 42, 0.12);
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 1vh;
  max-width: 42%;
}

.joystick-title {
  font-size: 24rpx;
  font-weight: 600;
  color: #1f2937;
  text-align: center;
}

.joystick {
  width: 25vh;
  height: 25vh;
  max-width: 240rpx;
  max-height: 240rpx;
  min-width: 180rpx;
  min-height: 180rpx;
  border-radius: 50%;
  background: radial-gradient(circle at 30% 30%, #eef2ff 0%, #c7d2fe 100%);
  position: relative;
  overflow: hidden;
}

.stick {
  width: 80rpx;
  height: 80rpx;
  border-radius: 50%;
  background: #1e293b;
  position: absolute;
  left: 50%;
  top: 50%;
  transform: translate(-50%, -50%);
}

.button-area {
  background: #ffffff;
  border-radius: 18rpx;
  padding: 1.2vh 1.2vw;
  box-shadow: 0 8rpx 18rpx rgba(15, 23, 42, 0.1);
}

.button-title {
  font-size: 22rpx;
  color: #475569;
  margin-bottom: 8rpx;
  text-align: center;
}

.button-row {
  display: flex;
  justify-content: center;
  gap: 1vw;
  margin-bottom: 8rpx;
}

.color-btn {
  flex: 1;
  max-width: 42%;
  padding: 12rpx 10rpx;
  border-radius: 14rpx;
  color: #fff;
  text-align: center;
  font-size: 22rpx;
  transition: transform 0.1s ease;
}

.color-btn.pressed {
  transform: translateY(4rpx);
  filter: brightness(0.85);
}

.color-btn.red {
  background: #ef4444;
}

.color-btn.blue {
  background: #3b82f6;
}

.color-btn.green {
  background: #22c55e;
}

.color-btn.yellow {
  background: #f59e0b;
}

.disconnect-btn {
  width: 100%;
  margin-top: 0;
  background: #ef4444;
  color: #fff;
}